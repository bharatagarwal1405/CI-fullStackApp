AWSTemplateFormatVersion: '2010-09-09'
Description: 'Full Stack Application CloudFormation Template'

Resources:
  CloudFormationBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: 'fullstackapp-cf-bucket'
      WebsiteConfiguration:
        IndexDocument: index.html
        ErrorDocument: error.html

Outputs:
  WebsiteURL:
    Value: !GetAtt CloudFormationBucket.WebsiteURL
    Description: URL of the S3 static website
  S3BucketSecureURL:
    Value: !GetAtt CloudFormationBucket.DomainName
    Description: Secure URL of the S3 bucket